<form method="post" action="{{ path('liv_address_delete', {'id': liv_address.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liv_address.id) }}">
    <button class="btn">Delete</button>
</form>
